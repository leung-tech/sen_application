# 全系統自動化回歸測試報告

## 測試範圍與狀態

本輪把「全系統」拆成兩個可追溯部分：第一部分是題庫、結構、模組載入與可及性的靜態稽核；第二部分是實際瀏覽器中的作答、完成、課堂摘要、努力星與安全退出流程。所有結果均以目前本地 Git 主分支版本為準。

| 測試層 | 狀態 | 現有結果 |
| --- | --- | --- |
| audit／verify 靜態稽核 | 已執行 | 38 項腳本以每項 15 秒上限分三批執行；28 項通過，10 項因舊版自動化工具逾時而待複核。 |
| JavaScript 語法 | 通過 | 已覆蓋 `modules/` 與 `assets/js/` 所有前端檔案。 |
| 一般遊戲作答與課堂摘要 | 通過 | `lessonGames`、`lessonCorrect`、`lessonHints`、`lessonRetries`、`addToken`、`gamesComplete` 與 `gameView` 的可見結果均已自動化驗證。 |
| 專屬實驗室完成回調 | 通過 | 已以形音義五星級工房完成事件驗證課堂摘要匯入與重設。 |

## 靜態稽核批次結果

| 批次 | 通過 | 逾時／待複核 | 重點 |
| --- | ---: | ---: | --- |
| 題庫與核心互動 | 7 | 5 | ADHD 分級、ASD 核心、EBD／MI、Gifted 跨類別與 Gifted 八項舊版工具需要個別複核。 |
| 跨類別、可及性與路線 | 7 | 5 | ID、跨類別 a11y、版面、SLI 核心及 SLI 八項舊版工具需要個別複核。 |
| SpLD、VI 與分學段驗證 | 14 | 0 | 包括五星級形音義、鍵盤、對比、手機、WCAG、初中及高中驗證均通過。 |

> 逾時不等同產品失敗。下一階段會以長時限診斷或直接瀏覽器測試區分「工具等待／選擇器失效」和真正的產品問題。

## 自動化互動測試執行備註

首次以單一長瀏覽器指令連續完成三回合作答時，瀏覽器自動化指令在 30 秒前逾時；重建乾淨頁面後，首頁與課堂摘要正常載入。後續互動測試會拆成較短的啟動、單次作答、完成及重設步驟，以免測試執行器的時間限制掩蓋頁面實際狀態。

## 最終回歸結果

所有 `modules/` 和 `assets/js/` 的 JavaScript 檔案已通過語法檢查。形音義五星級題庫、題目唯一性、初中 SpLD 與高中 SpLD 驗證亦再次通過；形音義的 20 個「學段 × 星級」組合均有 20 個不重複漢字與 4 個拆字示範。

| 自動化流程 | 實測輸入／動作 | 結果 |
| --- | --- | --- |
| 課堂摘要手動控制 | 啟動課堂模式，再各按一次提示、重試及努力星 | 通過；提示為 1、重試為 1、努力星顯示 1，且未作答前正確次數維持 0。 |
| 一般遊戲作答與完成 | 啟動「心情在哪裡？」並自動選取三回合的正確選項 | 通過；正確次數更新為 3、完成關卡更新為 1，完成後努力星累計為 2（原手動星加完成回饋星）。 |
| 一般遊戲 Escape | 在已完成的一般遊戲按 `Escape` | 通過；遊戲關閉，鍵盤焦點回到「從一般活動開始」按鈕。 |
| 課堂摘要重設 | 重設課堂模式本機記錄 | 通過；完成、正確、提示及重試均回復為 0。努力星是獨立鼓勵板，不會被課堂摘要重設清除。 |
| 形音義五星級完成回調 | 發出與五星級工房完成頁相同的 `spld-shape-sound-meaning-complete` 事件 | 通過；4 次正確、2 次重試、3 次提示及 1 個完成關卡均正確匯入課堂摘要，重設後全部清零。 |

## 已修正的產品問題

自動化測試發現一般活動按 `Escape` 後雖會返回主控台，但未明確把鍵盤焦點交還啟動該活動的按鈕。主接線現會記住一般活動的啟動控制；返回主控台時，若該控制仍存在，會將焦點交還給它。修正後的互動與計分回歸測試為 **0 項失敗**。

## 舊版稽核工具的維護狀態

38 項既有 audit／verify 工具以每項 15 秒上限執行時，有 28 項通過。10 項工具逾時，分別是 ADHD 分級、ASD 核心、EBD／MI、Gifted 跨類別、Gifted 八項、ID 感官、跨類別 a11y、全站版面、SLI 核心及 SLI 八項。延長執行 ADHD 分級工具後，它在讀取未定義的行動版結果時出現 `TypeError`，顯示至少其中一項是**測試工具的過時假設**，而不是產品斷言失敗。

這些舊工具需要下一輪改用目前的選擇器與回傳值；它們不會阻止本輪已完成的語法、題庫、一般遊戲計分與形音義完成回調驗證。然而，在這 10 項工具完成維護前，本報告不把結果表述為「所有歷史自動化工具均通過」。

## 發布判定

> **目前沒有偵測到會阻止發布的產品功能或計分問題。** 一般遊戲與形音義五星級工房的代表性完成／計分流程均已自動化通過，且修正後 Escape 焦點返回已回歸通過。

網站可作為發布候選版本；發布後仍應於實際公開網址進行一次桌面與手機煙霧測試。建議把 10 項舊版稽核工具維護列入下一個維護周期，以恢復全量舊測試的綠燈狀態。
