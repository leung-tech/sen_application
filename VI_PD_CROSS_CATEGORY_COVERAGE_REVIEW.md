# VI、PD 與跨類別入口覆蓋度檢視

本檢視以現有網站內容及本輪提出的互動方向為基礎。目標是保留現有的低壓、可重試、鍵盤可及及本機課堂支援設計，避免因為補強而把硬體、速度、肢體動作或個人表現變成參與門檻。

| 入口 | 已有內容 | 判定 | 本輪補強方向 |
|---|---|---|---|
| VI 視覺障礙 | 四學段各有兩項活動，涵蓋左右／前方定向、鍵盤定位、社區聲音、點字鍵位、文字聽讀、港鐵導航與結構化查閱。已有高對比、鍵盤與可選音訊。 | 大部分覆蓋 | 保留以文字及鍵盤為主的聲音定位；補入可選觸覺提示開關，並明確說明瀏覽器不會量度視力、方向感或使用者表現。 |
| PD 肢體傷殘 | 四學段各有兩項活動，涵蓋單鍵、掃描、節能策略、無障礙路徑、自我倡導、停留選取、智能家居及數碼職前策略。 | 大部分覆蓋 | 把「按住」描述改為單次啟動的微步驟，保留 Tab、Enter、點按及掃描替代；不假設使用者配有眼動儀或可持續按住按鍵。 |
| 情緒社交 | 已有表情、輪候、ASD 社交、HI 視覺溝通、EBD 調節、MI 選擇卡及雙贏策略桌。 | 部分覆蓋 | 新增跨類別「同理與協商工房」的八個虛構角色情境，集中於多角度理解、I-message、界線、修復、求助與不安全時的成人支持。 |
| 認知學習 | 已有分類、聽覺理解、數量、記憶、SpLD、ADHD 及 Giftedness 的個別活動。 | 部分覆蓋 | 新增跨類別「學習策略控制台」的八個虛構學習任務，集中於難度預測、分塊、主旨提取、外部提示、計劃、切換、核對與修訂。 |

## 安全及無障礙改寫

VI 的聲音方向提示必須同時有文字與大型方向控制；立體聲效果只屬可選聽覺線索。觸覺提示只在使用者主動開啟且裝置支援時才嘗試播放；未支援震動的裝置會保留完全等效的文字與視覺訊息。網站不會讀取視力資料、進行視覺能力校正或宣稱提供真實定向行動評估。

PD 的停留選取只提供瀏覽器可用的焦點停留與鍵盤替代，並不使用鏡頭、網絡攝影機或偽裝成眼動追蹤。所有練習可用點按、Tab、Enter 或單次空白鍵啟動；沒有長按門檻、反應時間失敗、扣分或疲勞分數。節能活動只提供課堂覺察與自我倡導短句，不會量度個人的體力或健康狀態。

兩個跨類別工房都使用虛構情境，不收集學生情緒、行為、能力或私人經驗。活動不排名、不建立人際分數、不判定人格、也不把策略選擇當成診斷或治療結果。遇到現實中感到不安全或難以處理的情況時，畫面只會提示告訴身旁可信任成人並按學校既有程序處理。

## 實作及驗證記錄

本輪已新增兩個跨類別入口。當使用者選擇頂部的「情緒社交」時，會先看到「同理與協商工房」；選擇「認知學習」時，會先看到「學習策略控制台」。兩者都有 8 張策略卡、準備頁、可選朗讀、策略提示、先停一停、數字鍵 `1` 至 `3`、Escape 離開和焦點返回。錯誤選擇只顯示「可以再比較」的提示，並留在同一張卡；完成不會寫入個人分數或能力資料。

| 驗證範圍 | 實測結果 |
|---|---|
| 情緒社交工房 | 卡片在篩選後正確出現；首張「積木桌的空位」以數字鍵 `2` 顯示溫和重新比較提示，以 `1` 進入第 `2 / 8` 張；Escape 正確離開。 |
| 認知學習控制台 | 卡片在篩選後正確出現，準備頁清楚列出難度預測、分塊、記錄、規劃、切換及修訂的八回合支架。 |
| PD 單次啟動 | 初小「單鍵太空熱氣球」的準備頁寫明可用單次空白鍵、Tab、Enter 或點按；按一次空白鍵高度由 `50%` 變為 `68%`，沒有持續按住要求。 |
| VI 對比與方向 | 初小「尋找糖果屋」可在音訊關閉下以文字方向與方向鍵操作；啟用黃字高對比和可選觸覺提示後，左方向鍵由第 `1 / 8` 題進入第 `2 / 8` 題。 |

靜態檢查已通過新跨類別模組、VI／PD 模組及主程式的 `node --check`；跨類別兩個工房各有 8 張策略卡，VI／PD 分齡活動維持 16 項，`git diff --check` 無格式錯誤。瀏覽器主控台沒有應用程式錯誤；測試期間唯一的語法訊息來自一次已修正的頁面測試指令，並非網站程式錯誤。
