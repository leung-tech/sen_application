# ASD、ADHD 與跨模組無障礙稽核

本文件記錄 ASD、ADHD 及共用 SEN 互動頁面的鍵盤、輔助科技語意與 375px 行動裝置稽核結果。稽核會區分原生按鈕、焦點移動、動態回饋、對話框語意、控制名稱、行動版溢出和最小觸控尺寸。

## SpLD 跨學段基準

初中八項及高中六項 SpLD 活動已全數通過自動化鍵盤及輔助科技語意模擬，作為後續檢視 ASD、ADHD 和共用遊戲的基準。每項均具可辨識對話框、焦點限制、Escape 關閉、回復至啟動卡、進度語意、狀態訊息及具名稱的低壓工具。

## 稽核範圍與方法

公開版本使用 Chromium 及 Chrome DevTools Protocol 進行可重複的鍵盤與輔助科技語意模擬。共用路線覆蓋 ASD、ID、Giftedness、HI、EBD、SLI 和 MI 的初小入口，另逐一覆蓋 ASD 的初小、高小、初中及高中四個學段；ADHD 則檢查分級專注實驗室的開啟、短衝刺題目、動態回饋與關閉流程。所有路線另以 **375 × 812px** 視窗檢查橫向溢出與最少 40px 的可操作控制尺寸。

| 範圍 | 路線／活動數 | 檢查內容 | 公開回歸結果 |
|---|---:|---|---|
| 共用情境活動 | 7 個路線入口 | 原生按鈕、可及名稱、提示、動態回饋、切換後焦點 | 通過 |
| ASD 社交情境 | 4 個學段 | 情境卡、策略卡、答案按鈕、提示與狀態回饋 | 通過 |
| ADHD 專注實驗室 | 4 個練習模式的共用對話框流程 | 對話框名稱、初始焦點、Tab／Shift+Tab、Escape、焦點回復、回饋、進度列 | 通過 |
| 375px 行動版 | 7 個共用路線及 ADHD 彈窗 | 橫向溢出、可見控制、最少 40px 控制尺寸 | 通過 |

## 已完成的跨模組修正

共用 `stageFrame()` 的 `#gameFeedback` 現在具有 `role="status"`、`aria-live="polite"` 和 `aria-atomic="true"`。因此 ASD、ID、Giftedness、HI、EBD、SLI、MI 及其他沿用此共用畫面的活動，在提示、正確回饋、重試及下一步更新時，均可把完整訊息傳遞給輔助科技。

一般遊戲由訓練大廳啟動後，焦點會移至可程式化聚焦的任務標題 `#stageTitle`，讓鍵盤及螢幕閱讀器使用者立即得知新任務名稱。ADHD 專注實驗室則已加入開啟後的關閉控制初始焦點、Tab／Shift+Tab 焦點循環、Escape 關閉、關閉後回復到「開啟專注挑戰」按鈕，以及具名稱與完整值域的進度列。其作答回饋亦已提升為完整狀態訊息。

## 自動化公開驗證結果

最終公開回歸測試的 `failureCount` 為 **0**。結果已保存於 `sen-cross-a11y-audit.json`；可重跑的測試程式為 [`scripts/audit-sen-cross-a11y.mjs`](scripts/audit-sen-cross-a11y.mjs)。初中與高中 SpLD 的 14 項直接選關測試則由 [`scripts/audit-spld-keyboard-sr.mjs`](scripts/audit-spld-keyboard-sr.mjs) 驗證，並同樣取得 `failureCount: 0`。

> 本稽核是針對 WCAG 2.1 AA 所需的鍵盤流程、可及名稱、動態訊息、對話框及進度語意之自動化模擬，並加上 375px 行動版操作檢查。建議每次新增互動模式後重新執行兩個稽核腳本，並於真實裝置配合常用輔助科技作課堂前抽樣測試。
